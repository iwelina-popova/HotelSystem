@model DateTime?


@if (Model.HasValue)
{
    @Html.TextBox("", String.Format("{0:mm/dd/yyyy}", Model.Value), new { @class = "textbox datepicker" })
}
else
{
    @Html.TextBox("", String.Format("{0:mm/dd/yyyy}", DateTime.UtcNow), new { @class = "textbox datepicker" })
}
